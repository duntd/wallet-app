import {AppText, Container} from 'components';
import React from 'react';

const Home = () => {
  return (
    <Container>
      <AppText>Home</AppText>
    </Container>
  );
};

export default Home;
