import {AppText, Container} from 'components';
import React from 'react';

const Menu = () => {
  return (
    <Container>
      <AppText>Menu</AppText>
    </Container>
  );
};

export default Menu;
